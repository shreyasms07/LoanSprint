services:
  - type: web
    name: loansprint-backend
    env: go
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app ./backend/cmd/server
    startCommand: ./app
    envVars:
      - key: PORT
        value: 8080
      - key: GIN_MODE
        value: release

# Made with Bob
